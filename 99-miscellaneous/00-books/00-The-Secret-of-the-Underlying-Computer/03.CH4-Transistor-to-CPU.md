# CH4: From Transistors to CPUs, Nothing Is More Important

## 4.1 This Small Toy Is Called a CPU

### 4.1.1 A Great Invention

### 4.1.2 AND, OR, NOT

### 4.1.3 The One Produces Two, Two Produces Three, Three Produces Everything
- All logical functions can be expressed using AND, OR, and NOT gates → logical completeness

### 4.1.4 Where Does Computational Power Come From?
- Binary addition
    - 0 + 0 = 0, carry 0
    - 0 + 1 = 1, carry 0 → XOR
    - 1 + 0 = 1, carry 0 → XOR
    - 1 + 1 = 0, carry 1 → AND

### 4.1.5 The Mysterious Ability to Remember
- NAND (NOT AND) = AND → NOT

### 4.1.6 The Birth of Registers and Memory

### 4.1.7 Hardware or Software? A General-Purpose Device

### 4.1.8 The Fundamental Technology of Hardware: Machine Instructions

### 4.1.9 The Interface Between Software and Hardware: Instruction Set

### 4.1.10 Circuits Need a Conductor
- clock signal
- Each time the clock signal changes voltage, every register and the entire circuit state are updated
- The clock rate represents how many times this change occurs per second

### 4.1.11 A Major Achievement: The Birth of the CPU!

## 4.2 What Does the CPU Do When It Is Idle?

### 4.2.1 What Is CPU Utilization?

### 4.2.2 Process Management and Scheduling
- The operating system assigns priorities to processes and places them into a queue so the scheduler can schedule them based on priority

```cpp
------------------------ ready queue
process   process   process                ----> CPU
------------------------                  |
^                                         |
|             ------------------------     |
I/O  <---- process    process        <-------
              ------------------------ blocking queue
```

### 4.2.3 Checking Queue State: A Better Design
- If the ready queue is empty, it means there are no processes for the OS to schedule and the CPU is in an idle state
- The queue can be kept non-empty so that the scheduler can always find a process to execute → System Idle Process
    - Always in the ready state
    - Has the lowest priority
    - When there are no schedulable processes, the scheduler runs this idle process